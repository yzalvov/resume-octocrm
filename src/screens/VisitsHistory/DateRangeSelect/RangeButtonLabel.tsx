import { Text } from 'grommet'
import { GLOBAL_LETTER_SPACING } from '../../../theme-custom'
import styled from 'styled-components'

export const RangeButtonLabel = styled(Text).attrs({
  weight: 500,
  style: { letterSpacing: GLOBAL_LETTER_SPACING }
})``
