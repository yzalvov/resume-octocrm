import styled from 'styled-components'
import { SecondaryRoundedContainer } from '../../components'

export const LongreadPaperContainer = styled(SecondaryRoundedContainer).attrs({
  pad: { horizontal: 'large', vertical: 'medium' },
  round: '.375rem'
})``
